# `modules/object_storage/r2/` — STUB

Not implemented in blueprint v0.4.0. The expected output contract is
the same as `modules/object_storage/aws/` — see that module's README.md
for the field list. PRs welcome.

`terraform plan` against this module fails fast with a clear message
so adopters who pick `var.cloud=r2` aren't left confused.
