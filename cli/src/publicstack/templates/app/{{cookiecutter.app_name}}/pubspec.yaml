name: {{ cookiecutter.app_name }}
description: "{{ cookiecutter.app_name }} app for this PublicStack Public Service."
publish_to: "none"
version: 0.0.1+1

environment:
  sdk: ^3.5.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  ui:
    path: ../../libraries/ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
