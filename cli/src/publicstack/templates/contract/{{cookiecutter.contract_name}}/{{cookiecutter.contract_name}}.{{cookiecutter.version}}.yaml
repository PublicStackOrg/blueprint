openapi: 3.1.0
info:
  title: {{ cookiecutter.contract_name }}
  version: {{ cookiecutter.version }}
  description: |
    PublicStack contract: {{ cookiecutter.contract_name }} ({{ cookiecutter.version }}).
    Fill in paths and components below.
  x-publicstack-contract-name: {{ cookiecutter.contract_name }}
  x-publicstack-contract-version: {{ cookiecutter.version }}
paths: {}
components:
  schemas: {}
